import React, { FC } from 'react';
import styles from './Footer.module.scss';

const Footer: FC = function () {
    return <footer className={styles.footer}>Футер</footer>;
};

export default Footer;
